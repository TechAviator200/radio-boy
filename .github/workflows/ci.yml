name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  backend:
    name: Backend (Python 3.11)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Compile-check backend source
        run: python -m compileall radio_boy_app.py chainlit/

  # NOTE: frontend is currently stored as a broken gitlink (mode 160000,
  # no .gitmodules). This job will fail until the frontend is either:
  #   a) properly registered as a git submodule, or
  #   b) converted to tracked files in the parent repo.
  # Uncomment once resolved.
  #
  # frontend:
  #   name: Frontend (Node 20)
  #   runs-on: ubuntu-latest
  #   defaults:
  #     run:
  #       working-directory: frontend
  #   steps:
  #     - uses: actions/checkout@v4
  #
  #     - uses: actions/setup-node@v4
  #       with:
  #         node-version: "20"
  #         cache: npm
  #         cache-dependency-path: frontend/package-lock.json
  #
  #     - run: npm ci
  #     - run: npm run build
